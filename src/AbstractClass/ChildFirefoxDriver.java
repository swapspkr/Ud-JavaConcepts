package AbstractClass;

public abstract class ChildFirefoxDriver extends FirefoxDriver {

	// we can create abstract methods in child abstract class (inheritance is possible in abstract class)
	// we can inherit the abstract class with abstract class or concrete class
}
